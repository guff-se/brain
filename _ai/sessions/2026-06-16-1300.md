# Brain session log — 2026-06-16 13:00

Locked in a consistency guard for consumed article filenames.

What changed:
- Documented the article filename rule in `_meta/pipelines.md`.
- Documented the same rule in `CLAUDE.md` for `/ingest`.
- Added `_ai/scripts/check_article_filenames.py`.
- The checker enforces the canonical pattern `YYYY-MM-DD-slug.md` with date precedence:
  - `published`
  - `created`
  - `captured`
  - `ingested`
- The checker now accepts specific article paths so it can be run on only the files touched in a session instead of the full legacy corpus.
- Verified the two recently added AI-related article notes pass the check.

Operational change:
- For new consumed article ingest, run the filename check on touched files before commit.
- Legacy filename mismatches remain backlog and should not be mixed into ordinary ingest work unless explicitly doing a cleanup pass.
