# Brain session log — 2026-06-16 10:16

Infrastructure reset for the Brain vault.

What changed:
- Confirmed `~/brain` is the canonical local git repo.
- Confirmed GitHub remains the source of truth (`origin`).
- Rebuilt the iCloud copy from `~/brain` after moving the old iCloud folder aside to `Brain.pre-rebuild-2026-06-16-1016`.
- Ensured the rebuilt iCloud copy does not contain `.git`.
- Disabled the earlier experimental two-way sync setup (`brain-sync` and Unison profile) after it produced widespread file-provider conflicts.
- Added a new one-way mirror script: `/Users/dante/bin/brain-mirror-to-icloud`.
- Tested a LaunchAgent approach for background mirroring, but it could not write to the iCloud folder because of macOS/File Provider permission constraints in that context.
- Installed git hooks in `~/brain/.git/hooks/` to run the mirror script after commit/merge/checkout/rewrite/applypatch instead.

Operational model now:
- Work in `~/brain`.
- Commit/push from `~/brain` to GitHub.
- iCloud copy is a mirror for mobile access, not a working git checkout.
- Manual mirror remains available via `/Users/dante/bin/brain-mirror-to-icloud`.

Follow-up:
- If the rebuilt iCloud mirror behaves normally, the parked backup folder can be deleted later.
